
dock load
  load <std:fs>, name fs

task write-file
  take path, like text
  take content, like text
  save r
    call fs/write
      bind path, read path
      bind content, read content
  call r/unwrap
  back text <done>

task read-file
  take path, like text
  save r
    call fs/read-to-string
      bind path, read path
  back call r/unwrap
