task fetch-data
  wait true
  take url, like text
  save response
    call fetch-url
      bind url, read url
      wait true
  send back, read response

task process
  take x, like u64
  send back, call add
    bind a, read x
    bind b, mark 1
