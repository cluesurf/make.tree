task safe-add
  take a, like u64
  take b, like u64
  send back, call add
    bind a, read a
    bind b, read b

task unchecked-add
  risk true
  take a, like u64
  take b, like u64
  send back, call add
    bind a, read a
    bind b, read b
